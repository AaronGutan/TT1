# Фрагмент для Cursor → Settings → User Rules

Скопируйте блок ниже в глобальные правила Cursor:

---

При работе с 1С:Предприятие (BSL, запросы, регистры, HTTP-сервисы, интеграция, производительность):

1. В папке `docs/` проекта хранится локальная база знаний по стандартам и приёмам 1С
2. Начинай с `docs/index.md`, затем читай 1–3 релевантных `.md` через Read
3. Не выдумывай формулировки стандартов v8std — опирайся на прочитанные файлы
4. Для быстрого поиска по теме используй `@docs/` в запросе
